# Dictionary — technical terms, proper nouns, product names
# One term per line. These terms are (a) seeded into faster-whisper as the initial_prompt
# (order = priority, Whisper accepts about 224 tokens) and (b) protected in the clean-up
# prompt as "spell exactly like this". Lines starting with # are comments.
#
# Put in whatever Whisper keeps getting wrong for you: product names, place names,
# abbreviations from your field, the names of the people you work with.

# Example: tools and systems
Nextcloud
Thunderbird
LibreOffice
Ollama
Whisper

# Example: places
Nienburg
Leipzig
Hannover

# === NUR-CLEANUP (everything below this marker line) ===
# Whisper's prompt only holds about 224 tokens, and a long list silently drops its tail — which is
# how a batch of names once stopped reaching Whisper without any warning. Terms below the marker are
# therefore NOT sent to Whisper; they only go to the clean-up model as "spell it exactly like this".
# Put the long tail here: project names, product names, jargon, anything Whisper usually gets close
# enough for the alias rules (aliases.txt, grown via --calibrate) to repair.
# The app prints how many terms went into each tier at startup.
Kubernetes
PostgreSQL
Grafana
WireGuard
Raspberry Pi
Home Assistant
Zigbee
Jitsi
Matrix
Paperless
